{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: clang++ build active file",
            "command": "/usr/bin/clang++", // Changed from clang to clang++
            "args": [
                "-std=c++17", // Specify C++17 standard
                "-stdlib=libc++", // Use the LLVM C++ standard library (optional but recommended for macOS)
                "-fcolor-diagnostics", // Keep existing flags
                "-fansi-escape-codes",
                "-g", // Generate debug information
                "${file}", // The file currently open in the editor
                "-o", // Specify output file name follows
                "${fileDirname}/${fileBasenameNoExtension}" // Output file path
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}
